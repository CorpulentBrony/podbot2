import * as ApiRequestCommands from "./ApiRequestCommands";

export class ass extends ApiRequestCommands.youtube {
	static exec(message) { return super.exec(message, "ySEbw4come0"); }
}
ass.ALIASES = [];

export class ntt extends ApiRequestCommands.fourchan {
	static exec(message) { return super.exec(message, "nightly twilight"); }
}
ntt.ALIASES = [];

export class plush extends ApiRequestCommands.fourchan {
	static exec(message) { return super.exec(message, "plush"); }
}
plush.ALIASES = [];